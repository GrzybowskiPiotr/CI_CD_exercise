name: E2E tests
description: Run E2E tests

inputs:
  baseUrl:
    description: 'Base URL for the E2E tests'
    required: true
  test-url:
    description: TEST url pass to action""
    required: true

runs:
  using: 'composite'
  steps:
    - name: Run E2E test
      run: |
        echo "Running E2E tests with base URL: ${{ inputs.test-url }}"
      shell: bash
