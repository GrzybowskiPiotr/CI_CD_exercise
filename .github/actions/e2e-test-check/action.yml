name: E2E tests
description: Run E2E tests

inputs:
  baseUrl:
    description: 'Base URL for the E2E tests'
    required: true
  test-url:
    description: 'TEST url pass to action'
    required: true

runs:
  using: 'composite'
  steps:
    - name: Show env
      run: |
        echo "Base URL: $DEPLOY_URL"
      shell: bash
